type: pages
label: Books
parent: kirby.page("books")
info: "{{ page.published }}"
template: book
empty: No books yet
sortBy: date desc
image:
  query: page.files.filterBy("type", "image").first()
  cover: true
  ratio: 3/2
